[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-utility"
version = "0.0.1"
authors = [
  { name="Ryuto Yoshida"},
]
description = "Utilities for OpenCV flags and helpers, including modern USAC variants."
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "opencv-contrib-python",
]

[project.urls]
"Homepage" = "https://github.com/ry-yoshida-private/OpenCVUtility"
"Bug Tracker" = "https://github.com/ry-yoshida-private/OpenCVUtility/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
opencv_utility = ["py.typed"]
